#include <stdio.h>
#include <stdlib.h>

enum Status { CONTINUE, WIN, LOST}; //! -> costante di enumerazione che crea un "nuovo tipo"

int main(){
     
     enum Status gameStatus; //* -> crea la variabile gameStatus di tipo Status 


}