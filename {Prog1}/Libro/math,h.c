#include <stdio.h>
#include <math.h>

int main(){

//! tutte le funzioni che fanno parte della libreria math.h restituiscono un valore double

printf("%.2f\n", sqrt(900)); //-> calcola la radice quadrata sqrt(x)
printf("%f", pow(5, 2)); // -> calcola la potenza di tra due numeri: x^y = pow( x, y)
ceil(x) //-> arrotonda x all'intero più piccolo non minore di x
floor(x) //-> arrotonda x all'intero più grande non maggiore di x

//! Un programma con molte funzioni scritte in maniera corretta è più riutilizzabile e più efficiente
}