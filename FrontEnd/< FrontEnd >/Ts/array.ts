let person : {name: string, age: number} = {name:"John", age:25};

let nameM = person.name.toUpperCase();
console.log(nameM);

const numbers = [1,2,3,4];
//oppure
const doubleID = numbers.map(function(item){
		return item*2
})

