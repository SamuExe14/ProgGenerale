#include <stdio.h>

int main()
{
    int grade = 90;
    puts(grade >= 60 ? "Passed" : "Failed");
//! In C è possibile implementare l'operatore ternario con ? e :

    if (grade = 60)
    {
        puts("Passed");
    }
    else if (grade > 80)
    {
        puts("Passed with +");
    }
    

    while (grade > 10)
    {
        grade--;
        printf("%d\n", grade);
    }
}