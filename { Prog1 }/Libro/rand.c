#include <stdio.h>
#include <stdlib.h> //! contiene la funzione rand() al suo interno

int main()
{
     int i = rand(); //-> assegna un valore a i da 0 a RAND_MAX
     int x = 1 + rand() % 6; //-> assegna un valore a x da 0 a 6
     
     
}
